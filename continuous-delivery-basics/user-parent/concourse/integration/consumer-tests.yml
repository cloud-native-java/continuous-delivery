---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: maven
    tag: alpine
inputs:
- name: user-integration
run:
  path: sh
  args:
  - -exc
  - |
    cd user-integration/continuous-delivery-basics/user-parent/cd-user-microservice \
    && mvn clean install -DskipTests \
    && cd ../user-consumer-tests \
    && mvn clean package
